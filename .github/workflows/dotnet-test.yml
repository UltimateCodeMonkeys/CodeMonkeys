name: Unit tests for branch 'master'
on:
  push:
    branches:
    - master
    
jobs:
  run-dotnet-test:
    name: Unit Tests
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2.1.0
      - run: dotnet test
